# 🔗 URL Shortener

Een eenvoudige maar volledig werkende URL shortener gebouwd in **Python**, met **Flask** en een lichte **SQLite-database**.  
Het project laat gebruikers een lange URL ingeven en genereert automatisch een korte code die naar dezelfde website redirect.

---

## 🚀 Functionaliteiten

- Lange URL → korte URL conversie  
- Automatische opslag in een SQLite database (`url.db`)
- Redirect naar originele URL via korte code
- Eenvoudige HTML-templates voor invoer en resultaat
- Minimalistische codebase, ideaal voor leren/praktijk
